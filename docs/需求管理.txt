使用初始需求文档和生成需求文档两个层次进行需求管理；
用户需要提供包含详细需求场景的初始需求文档，格式为yaml格式，并且定义需要的交互过程（该交互过程必须经由手头操作，确认流程正确），以及需要的整体页面截图和组件截图
agent按照用户提供的初始需求文档，打开url页面，进行相应截图操作，获取网页css和tsx代码，撰写根据结果获得的最终需求文档
该需求文档可以直接用于interface_designer，继续开发

需求文档以页面为单位进行组织
需求文档内部以功能组件为单位进行树状组织
agent生成的需求文档按照同样形式进行组织